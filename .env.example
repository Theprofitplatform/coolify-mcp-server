# Coolify MCP Server - Environment Configuration
# Copy this file to .env and fill in your actual values

# ==============================================================================
# COOLIFY CONFIGURATION (Required)
# ==============================================================================

# Your Coolify instance URL
# Example: https://coolify.example.com
COOLIFY_BASE_URL=https://your-coolify-instance.com

# Your Coolify API token
# Get this from: Coolify Dashboard → Security → API Tokens → Create Token
# IMPORTANT: Keep this secret! Never commit the actual token to git
COOLIFY_TOKEN=your-coolify-api-token-here

# ==============================================================================
# N8N CONFIGURATION (Optional)
# ==============================================================================

# Your N8N instance URL (for auto-deploy setup)
# Example: https://n8n.example.com
N8N_URL=https://your-n8n-instance.com

# ==============================================================================
# QDRANT CONFIGURATION (Optional - for infrastructure knowledge graph)
# ==============================================================================

# Qdrant host (DNS/FQDN, Docker container name, or IP)
# Example: qdrant.example.com or 10.0.0.5
QDRANT_HOST=your-qdrant-host.example.com

# Qdrant port
# Default: 6333
QDRANT_PORT=6333

# Qdrant API key
# Get this from your Qdrant container environment variables
QDRANT_API_KEY=your-qdrant-api-key-here

# OpenAI API key (optional - for better semantic search)
# If not provided, will use simple hash-based embeddings
# Get from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your-openai-api-key-here

# ==============================================================================
# ADDITIONAL CONFIGURATION (Optional)
# ==============================================================================

# Debug mode - set to 'true' to enable verbose logging
# DEBUG=false

# API Request Configuration
# Timeout for API requests in milliseconds (default: 30000)
COOLIFY_API_TIMEOUT=60000

# Maximum number of retries for failed requests (default: 3)
COOLIFY_API_MAX_RETRIES=5

# Delay between retries in milliseconds (default: 2000)
COOLIFY_API_RETRY_DELAY=2000

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
#
# 1. NEVER commit .env file to version control
# 2. Add .env to .gitignore (already done)
# 3. Rotate API tokens regularly
# 4. Use different tokens for different environments
# 5. Limit token permissions to only what's needed
#
# ==============================================================================
# HOW TO GET YOUR COOLIFY TOKEN
# ==============================================================================
#
# 1. Log into your Coolify instance
# 2. Go to Settings → Security → API Tokens
# 3. Click "Create Token"
# 4. Give it a descriptive name (e.g., "MCP Server")
# 5. Select permissions (at minimum: read/write applications)
# 6. Copy the token and paste it above
# 7. Save this file and NEVER share it
#
# ==============================================================================
# TESTING YOUR CONFIGURATION
# ==============================================================================
#
# After setting up your .env file, test it with:
#
#   npm run build
#   node build/index.js
#
# Or test the API connection:
#
#   curl -H "Authorization: Bearer $COOLIFY_TOKEN" \
#        "$COOLIFY_BASE_URL/api/v1/version"
#
# ==============================================================================
