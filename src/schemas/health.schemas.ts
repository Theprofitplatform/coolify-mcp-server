/**
 * Health and Version Tool Validation Schemas
 * Zod schemas for health/version operations
 */

import { z } from 'zod';

export const GetVersionSchema = z.object({});

export const HealthCheckSchema = z.object({});
